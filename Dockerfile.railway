# Use official Open WebUI image from Docker Hub (no egress limits)
FROM ghcr.io/open-webui/open-webui:main

# Railway injects PORT env var
ENV PORT=8080
ENV HOST=0.0.0.0

EXPOSE 8080

# Use the existing CMD from the base image
