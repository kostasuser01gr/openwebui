# =============================================================================
# Open WebUI — Production Environment (Hosted Models Only)
# =============================================================================
# INSTRUCTIONS:
#   1. Copy this file:  cp .env.hosted.example .env.hosted
#   2. Fill in your API key below
#   3. Generate a new secret key:  openssl rand -base64 32
#   4. Start:  docker compose -f docker-compose.hosted.yml --env-file .env.hosted up -d
# =============================================================================

# ─── MODEL PROVIDER ──────────────────────────────────────────────────────────
# DEFAULT: OpenRouter (OpenAI-compatible). Supports 200+ models.
# IMPORTANT: URL must end with /v1 (not /v1/ and not without /v1)
OPENAI_API_BASE_URL='https://openrouter.ai/api/v1'

# Your OpenRouter API key (get one at https://openrouter.ai/keys)
# NEVER commit this value to git!
OPENAI_API_KEY='sk-or-v1-REPLACE_WITH_YOUR_ACTUAL_KEY'

# ─── FALLBACK: GROQ (uncomment to switch) ────────────────────────────────────
# OPENAI_API_BASE_URL='https://api.groq.com/openai/v1'
# OPENAI_API_KEY='gsk_REPLACE_WITH_YOUR_GROQ_KEY'

# ─── AUTH & SECURITY ─────────────────────────────────────────────────────────
# Generate with: openssl rand -base64 32
# CRITICAL: Must remain stable across restarts or all JWT tokens invalidate
WEBUI_SECRET_KEY='REPLACE_ME_WITH_openssl_rand_base64_32_OUTPUT'

# Require login (true = mandatory)
WEBUI_AUTH=true

# Disable open registration (admin creates accounts manually)
ENABLE_SIGNUP=false

# ─── PORT ─────────────────────────────────────────────────────────────────────
# Host port for the web UI (container always listens on 8080 internally)
OPEN_WEBUI_PORT=3001

# ─── CORS / PROXY ────────────────────────────────────────────────────────────
# For reverse proxy: set to your domain. For local dev: * is acceptable.
CORS_ALLOW_ORIGIN='*'
FORWARDED_ALLOW_IPS='*'

# ─── TELEMETRY (disabled) ────────────────────────────────────────────────────
SCARF_NO_ANALYTICS=true
DO_NOT_TRACK=true
ANONYMIZED_TELEMETRY=false
